{
  "name": "cycling-game",
  "version": "0.1.0",
  "description": "Vélo Manager / Rider — POC",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently \"vite\" \"wait-on http://localhost:5173 && electron .\"",
    "build": "vite build && electron-builder",
    "vite": "vite",
    "electron": "electron ."
  },
  "dependencies": {
    "pixi.js": "^7.4.2",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "concurrently": "^8.2.0",
    "electron": "^42.0.0",
    "electron-builder": "^26.15.2",
    "vite": "^6.4.2",
    "vite-plugin-electron": "^0.28.0",
    "wait-on": "^7.2.0"
  }
}
